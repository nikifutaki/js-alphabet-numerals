version: 2.1
jobs:
  test:
    docker:
      - image: cimg/node:17.2.0
    steps:
      - run:
          command: npm test
          name: Test
workflows:
  version: 2.0
  test:
    jobs:
      - test